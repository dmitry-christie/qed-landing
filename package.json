{
  "name": "qed-frontend",
  "private": true,
  "version": "1.0.0",
  "description": "QED landing pages (static) + Netlify lead-capture functions (Telegram).",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "netlify dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.1",
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
